//Take the button of the div for go to the next and save it in a variable
let anterior = document.getElementById("botones").childNodes[1];

//Take the button of the div for go  back and save it in a variable
let siguiente = document.getElementById("botones").childNodes[3];

let arrayPalabras = [];
//Event click for go back
anterior.addEventListener("click", ()=>{
    for (let i = 0; i < array.palabvras; i++) {
        const element = array[i];
        
    }
});

//Event click for go next
siguiente.addEventListener("click", ()=>{
    
});